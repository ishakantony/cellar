import type { FeatureModule } from '@cellar/shell-contract';

/**
 * Lazy Vault module skeleton. Real routes/nav land in issue #003.
 */
const featureModule: FeatureModule = {
  routes: [],
  nav: [],
};

export default featureModule;
