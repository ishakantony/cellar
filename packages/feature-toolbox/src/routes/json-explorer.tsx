/**
 * Placeholder JSON Explorer page (issue #008).
 *
 * The real implementation lands in issue #010 (CodeMirror-backed editor + tree
 * view). This stub exists so the rail + lazy-load + error-boundary contract
 * has a real second feature to validate against.
 */
export function JsonExplorerPage() {
  return (
    <section className="max-w-lg space-y-2">
      <h1 className="text-xl font-bold tracking-tight text-slate-100">JSON Explorer</h1>
      <p className="text-xs text-outline">Under construction</p>
    </section>
  );
}
